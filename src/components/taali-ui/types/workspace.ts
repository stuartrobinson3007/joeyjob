/**
 * Frontend Workspace Types
 * 
 * Use direct tRPC calls to get workspace data with full type safety.
 * The workspace hook now only handles state management.
 */

// Router type available via direct app import in trpc.ts